

# Fix Separator Visibility

**File:** `src/app/(public)/landing/page.tsx`, line 66

**Change:** Update `opacity-25` to `opacity-50` on the `<hr>` element to make it more visible against the `#f8fafc` background.

```
// Before
<hr className="mt-3 mb-4 opacity-25" />

// After
<hr className="mt-3 mb-4 opacity-50" />
```

No other files changed.

